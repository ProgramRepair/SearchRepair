int test(int ighprv, int q, int cluster, int seg_size, int igh){
if ( igh   && ighprv   && cluster     )     { if ( seg_size   +   <missing ')'>          bv  bv_len >  queue_max_segment_size ( q       )    )   goto  new_segment ;  if ( IOVEC_PHYS_MERGEABLE ( vprv       , v       )       )          goto  new_segment ;  if ( IOVEC_SEG_BOUNDARY ( q       , vprv       , v       )       )          goto  new_segment ;  seg_size +=  bv   bv_len      ;  bvprv = bv        ;   continue ;  }    }