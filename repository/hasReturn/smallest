void smallest(float a, float b, float c, float d, float m)
{
    if(a <= b && a <= c && a <= d) 
    	m = a;
    else if(b <= a && b <=c && b <= d)
    	m = b;
    else if(c <= a && c <= b && c <= d)
    	m = c;
    else{
    	m = d;
    }
    

}