void median(int x, int y, int z, int m)
{
    if((x < y && x > z) || (x > y && x <z)) 
    	m = x;
    else if((y < x && y > z) || (y > x && y < z))
    	m = y;
    else if(z > x && z < y || z <x && z > y)
     m = z;
    
}