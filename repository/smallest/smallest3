void smallest(int a, int b, int c, int d, int m)
{
    if(a < b && a < c && a < d)
        m = a;
    else if(b < a && b <c && b < d)
        m = b;
    else if(c < a && c < b && c < d)
        m = c;
    else if(d < a && d <= b && d <= c)
        m = d;    
}